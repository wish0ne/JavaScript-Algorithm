n = int(input())

d =[0] * n
d[0] = 1
d[1] = 3
for i in range(1, n):
    d[i] = (d[i - 1] + 2 * d[i - 1]) % 796796

print(d[n - 1])


# not solved 😥
# dp 문제 -> 점화식을 세워보자!⭐

# 이 문제의 점화식은 Ai = Ai-1 + Ai-2 * 2
# i번쨰 위치에 대한 최적해를 구할때 i-3번째 이하의 위치에 대한 최적해에 대해서는 고려할 필요가 없다.
# => 사용할 수 있는 덮개의 형태가 최대 2x2 크기의 직사각형 형태이기 때문이다.
# 왼쪽부터 i-1까지 덮개가 이미 채워져 있으면 하나의 경우만 가능 (2x1짜리 사용)
# 왼쪽부터 i-2까지 덮개가 이미 채워져 있으면 두가지 경우만 가능 (2x2, 1x2짜리 2개 사용. 2x1 2개를 사용하는 경우는 이미 고려되었음.)
